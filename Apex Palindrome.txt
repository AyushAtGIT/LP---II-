public class Palindrome {
    public static void evenMethod()
    {
        String str = 'Arvind';
String revStr = str.reverse();
system.debug('Reversed string value is ' + revStr);
if (str== revStr ) 
{  system.debug('String is Palindrome :'+revStr ); 
}else 
{ 
system.debug('String is not Palindrome :'+revStr ); 
}

}
}